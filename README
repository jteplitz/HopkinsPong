In order to deploy this code you need to have a working mongodatabase. Once you have that you need to go to the server.js file and change the mongoose.connect parameter on line 49 to the url of your database. 
Or if you're using heroku just set an environment config variable aclled databaseURL.
